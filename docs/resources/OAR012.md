Path parameter names, query parameter names, object names, and property names with more than two words must follow the configured naming convention. You can configure snake_case, kebab-case (default), camelCase or UpperCamelCase

Noncompliant Example (OpenAPI 2):
---------------------------------
swagger: "2.0"
info:
  title: Swagger Petstore
  version: "1.0"
paths:
  /pets/id{pet-id}:
    get:
      parameters:
        - in: path
          name: pet-id
          type: integer
          required: true
      responses:
        200:
          description: Ok

Compliant Example (OpenAPI 2):
-------------------------------
swagger: "2.0"
info:
  title: Swagger Petstore
  version: "1.0"
paths:
  /pets/{pet-id}:
    get:
      parameters:
        - in: path
          name: pet-id
          type: integer
          required: true
      responses:
        200:
          description: Ok

Noncompliant Example (OpenAPI 3):
---------------------------------
openapi: "3.0.0"
info:
  title: Swagger Petstore
  version: "1.0"
paths:
  /pets/pet_{pet-id}:
    get:
      parameters:
        - in: path
          name: pet-id
          schema:
            type: integer
          required: true
      responses:
        200:
          description: Ok

Compliant Example (OpenAPI 3):
-------------------------------
openapi: "3.0.0"
info:
  title: Swagger Petstore
  version: "1.0"
paths:
  /pets/{pet-id}:
    get:
      parameters:
        - in: path
          name: pet-id
          schema:
            type: integer
          required: true
      responses:
        200:
          description: Ok
